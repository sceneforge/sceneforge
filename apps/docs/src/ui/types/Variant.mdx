import { Meta, Title, IconGallery, IconItem } from '@storybook/blocks';

<Meta title="@sceneforge|ui/Types/Variant" />

# Variant Type

`Variant` is an enum with the following options:

<table>
  <thead>
    <tr><th> Key     </th><th> Value     </th></tr>
  </thead>
  <tbody>
    <tr><td> Accent  </td><td> `accent`  </td></tr>
    <tr><td> Danger  </td><td> `danger`  </td></tr>
    <tr><td> Default </td><td> `default` </td></tr>
    <tr><td> Info    </td><td> `info`    </td></tr>
    <tr><td> Primary </td><td> `primary` </td></tr>
    <tr><td> Success </td><td> `success` </td></tr>
    <tr><td> Warning </td><td> `warning` </td></tr>
  </tbody>
</table>

## Import

```ts
import { Variant } from "@sceneforge/ui";
```

## Usage

```ts
import { Variant } from "@sceneforge/ui";

const defaultVariant = Variant.Default;
```
